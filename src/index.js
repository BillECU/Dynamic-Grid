import React from "react";
import ReactDOM from "react-dom/client";
import DynamicGrid from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <DynamicGrid />
  </React.StrictMode>
);
